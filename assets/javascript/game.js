$( document ).ready(function() {
//declaring variables for the game
var hp = 99;
var basehit = undefined;
var computerhit = undefined;
var opponents = [];
var defender;
var player;
var pselected = false;
var defselected = false;

function attack(min, max) {
    return Math.floor(Math.random() * (35 - 1 + 1) ) + 1;
    console.log();
  }

});
